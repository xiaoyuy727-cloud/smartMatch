信息

支持excel直接导入和手动加学生/老师信息

学生：序号	姓名	性别	年级	科目	家庭情况与学习情况（薄弱学科、具体知识短板、（学习风格（视觉型/听觉型/动手型）、兴趣爱好、性格特点）	社工姓名	社工联系电话	是否优先 特殊需求	

老师：序号 姓名 性别 学号 学邮 院系（专业） 辅导方式线上线下 匹配需求（预匹配还是直接匹配） 之前是否参加过 个人性格和风格（耐心吗，活泼吗，严肃吗） 对性别的要求  
偏好学生的科目（1位，2位，3位） 学段（1位，2位，3位）   



打分阶段

我的想法是，对每一对合法的师生对进行打分。首先什么是合法：性别和线上线下这两个强制要求会过滤掉不合法师生对，剩下的都是合法。然后每一对师生的打分维度有年级偏好匹配度（如果学生的年级和志愿者的第一顺位匹配，则3分，第二顺位2分，第三1分，不是则0分）/科目需求匹配度（如果学生的科目需求与志愿者的第一顺位匹配，则3分，第二2分，第三1分，不是则0分）（这里的分数设置目前是拍脑袋的，可以后期实验调整）/学习风格、性格与教学风格匹配度/志愿者专业与学生情况匹配程度/特殊需求匹配度。后两个维度目前也是拍脑袋的，我目前考虑设计打分表之后调用api来打分。

目前的想法是用struct存师生信息，然后n*m遍历，存储每个合法对。之后遍历合法对，通过调用deepseekapi的方式对主观维度打分，通过公式计算每一对的最终分数并存储。


匹配阶段
从匹配度高到低进行匹配。如果当前的匹配对中，小朋友是优先匹配的，那么选择该对，并在师生集合中分别把两人都删掉，并把这一对在排序中删掉。如果当前的匹配对中，小朋友或者老师已经不在师生集合，那么把这一对删掉。以这个规则对所有匹配对进行遍历。

遍历一遍之后，如果仍然剩余匹配对，那么继续从高到低进行匹配，这次都不是优先匹配了。对于当前的匹配对，选择该对，并在师生集合中分别把两人都删掉，并把这一对在排序中删掉。如果当前的匹配对中，小朋友或者老师已经不在师生集合，那么把这一对删掉。以这个规则对所有匹配对进行遍历。

第二次遍历之后这个匹配对的大根堆就空了。


打分-匹配流程，先对直接匹配的老师做。
如果还有剩下的学生，那么对剩下的学生和预匹配的老师做打分-匹配。


浏览器请求被拦住了


基础分的思路。我们先分为4部分，其中3部分构成总分100分，权重可以调整，先定grade-20分，subject-40分，教学风格（）-40分，然后特殊需求总分20分，在100分之外，对不符合特殊需求的匹配对进行扣除，如果没有特殊需求就视为0。然后，grade，按照符合的程度分别给100%，66.7%，33.3%的分数；subject，给老师的优先级从高到低负分3/2/1，学生赋分3/2/1，然后对于每一学科进行计算，公式是老师优先级分数*学生优先级分数，这样最高9分，最低0分，我们对subject这里的40分分成9份给分。然后教学风格和特殊需求的具体评分标准空着，后面deepseek做。


启动方式：
for windows
Set-ExecutionPolicy -Scope Process -ExecutionPolicy RemoteSigned
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
uvicorn main:app --reload

In 2nd cmd:
python -m http.server 5500

wsl指令略用不同，参阅AI.

http://127.0.0.1:5500/students.html
http://127.0.0.1:5500/volunteers.html
http://127.0.0.1:5500/matching.html

0221更新：改正预览上传数据的bug，优化了图形界面。建议之后的match算法直接从数据库db寻找数据，然后完成接下来的算法，可以输出match之后的excel.等算法完成后继续优化match的网页界面。


补充匹配结果落库部分：

-代码结构

匹配与调度（0117-0125）/
├─ backend/
│  ├─ models.py                              [修改]
│  │  ├─ 新增 MatchJob 表（一次完整匹配任务）
│  │  └─ 新增 MatchResult 表（最终匹配结果明细）
│  │
│  ├─ matching_service.py                    [修改]
│  │  ├─ 保持“单池匹配”职责（候选生成 + 两遍贪心）
│  │  ├─ 支持 student_ids 参数（用于 pre 阶段只处理剩余学生）
│  │  ├─ 在匹配结果中补充 round_type（priority/normal）
│  │  └─ 在匹配结果中补充 match_order（匹配顺序，便于复盘）
│  │
│  ├─ matching_orchestrator.py               [新增]
│  │  ├─ Stage1 核心编排器（direct -> pre）
│  │  ├─ 创建/更新 MatchJob 状态（running/success/failed）
│  │  ├─ 调用 matching_service 执行两阶段匹配
│  │  ├─ 汇总基础统计 summary（阶段统计 + 总统计）
│  │  └─ 调用 matching_store 落库
│  │
│  ├─ matching_store.py                      [修改/填充]
│  │  ├─ create_job() 创建匹配任务
│  │  ├─ save_match_results() 批量写入 MatchResult
│  │  ├─ finish_job_success()/finish_job_failed()
│  │  ├─ list_jobs() 历史批次列表查询
│  │  └─ get_job_detail() 批次详情查询（结果+统计）
│  │
│  ├─ matching_schemas.py                    [修改]
│  │  ├─ 保留现有 preview/run 的 schema
│  │  ├─ 新增 RunFullMatchOut（运行完整匹配返回）
│  │  ├─ 新增 MatchJobListItemOut（历史批次列表项）
│  │  ├─ 新增 MatchJobDetailOut（某批次详情）
│  │  └─ 新增 Summary/StageStats 相关 schema
│  │
│  ├─ main.py                                [修改]
│  │  ├─ 新增 POST /api/match/v1/run-full
│  │  ├─ 新增 GET  /api/match/jobs
│  │  ├─ 新增 GET  /api/match/jobs/{job_id}
│  │  ├─ （可选）扩展根路由说明，加入新接口
│  │  └─ 保持导入/列表/删除接口不受影响
│  │
│  ├─ database.py                            [通常不改 / 可能微调]
│  │  └─ 一般无需改；若你想补统一 JSON 序列化工具或 session 辅助可选改
│  │
│  ├─ app.db                                 [运行后变化]
│  │  └─ 新增表数据会写入（无需手动编辑）
│  │
│  └─ requirements.txt                       [通常不改]
│     └─ Stage1 不引入新三方库的话可不改
│
├─ frontend/
│  ├─ matching.html                          [修改]
│  │  ├─ 新增“运行完整匹配（run-full）”按钮
│  │  ├─ 新增“本次运行摘要”展示区（基础统计）
│  │  ├─ 新增“历史批次列表”展示区
│  │  ├─ 新增“查看批次详情”交互（调用 /api/match/jobs/{id}）
│  │  └─ 新增“批次详情结果表”展示区（最终匹配结果）
│  │
│  ├─ students.html                          [通常不改]
│  │  └─ Stage1 目标不涉及学生页逻辑
│  │
│  ├─ volunteers.html                        [通常不改]
│  │  └─ Stage1 目标不涉及志愿者页逻辑
│  │
│  └─ （可选）match_jobs.html                [新增，可选]
│     └─ 如果你想把“历史批次/详情”从 matching.html 拆成独立页面
│
└─ 开发记录.md                               [建议修改]
   └─ 记录 Stage1 的接口约定、summary字段、表结构说明（方便协作）

1、capacity目前是1.
2、greedy不是全局最优
3、单个师生对只补习一个科目


继续加入deepseek打分
文件结构：

匹配与调度（0117-0125）/
├─ backend/
│  ├─ models.py                                   [修改，建议重建库]
│  │  ├─ 扩展 MatchResult：增加分维度主观评分字段（style/major/special）
│  │  ├─ 增加每个维度的 reason/confidence/prompt_version 字段
│  │  └─ （可选）增加 llm_raw_json 字段（便于调试）
│  │
│  ├─ matching_rules.py                           [修改]
│  │  ├─ 保留客观分：grade_score / subject_score
│  │  ├─ 新增“主观分聚合函数”（teaching_style + major -> style_score）
│  │  ├─ 总分公式改为：base + subjective + penalty
│  │  └─ 保持合法性判断逻辑不变（mode / gender）
│  │
│  ├─ matching_service.py                         [修改]
│  │  ├─ 候选对评分时调用 DS 主观评分管道（对每个合法 pair）
│  │  ├─ 将 3 个 prompt 的结果合并回 score 字段
│  │  ├─ 支持 llm_enabled 参数（true/false）
│  │  ├─ 支持并发控制 / 超时 / 降级（稳定性，不是限量）
│  │  └─ 收集 llm_stats（调用数、超时数、失败数）
│  │
│  ├─ matching_orchestrator.py                    [修改]
│  │  ├─ run_full_match_v1 增加 llm_enabled 与 DS配置参数透传
│  │  ├─ direct / pre 两阶段都可启用 DS 评分
│  │  ├─ 合并 direct/pre 的 llm_stats 写入 summary_json
│  │  └─ 流程保持不变：direct -> pre -> 入库
│  │
│  ├─ matching_store.py                           [修改]
│  │  ├─ save_match_results() 写入新增的 DS 分维度字段
│  │  ├─ get_job_detail() 返回 llm_reason / confidence 等字段
│  │  └─ list_jobs() 可增加 llm_enabled / llm_stats 摘要透出
│  │
│  ├─ matching_schemas.py                         [修改]
│  │  ├─ CandidatePairOut / MatchPairOut 新增：
│  │  │   ├─ teaching_style_score
│  │  │   ├─ major_match_score
│  │  │   ├─ llm_reason_style / major / special
│  │  │   ├─ llm_confidence_style / major / special
│  │  │   └─ llm_status
│  │  ├─ RunFullMatchOut.summary 新增 llm_stats
│  │  └─ 保持 Stage1 字段兼容（旧字段仍可用）
│  │
│  ├─ main.py                                     [修改]
│  │  ├─ /api/match/v1/preview 新增 DS 开关参数
│  │  ├─ /api/match/v1/run 新增 DS 开关参数
│  │  ├─ /api/match/v1/run-full 新增 DS 开关参数（Stage2重点）
│  │  ├─ （可选）新增 DS health 接口（测试配置）
│  │  └─ 将参数传给 matching_service / orchestrator
│  │
│  ├─ ds_client.py                                [新增，建议]
│  │  ├─ 纯 HTTP 客户端：负责请求 DeepSeek API
│  │  ├─ 超时、重试、异常捕获
│  │  ├─ 响应文本提取
│  │  └─ 不承载业务逻辑（只做“客户端”）
│  │
│  ├─ ds_prompt_templates.py                      [新增，强烈建议]
│  │  ├─ prompt_style_match()      教学风格/性格匹配 prompt
│  │  ├─ prompt_major_match()      专业匹配 prompt
│  │  ├─ prompt_special_needs()    特殊需求匹配 prompt
│  │  ├─ 统一 JSON 输出格式约束（每个 prompt 各自 schema）
│  │  └─ 维护 prompt_version 常量
│  │
│  ├─ ds_scoring_service.py                       [新增，核心]
│  │  ├─ 对单个合法 pair 执行“三次 DS 调用”（分字段 prompt）
│  │  ├─ 解析三个 JSON 输出，做结构校验与范围裁剪
│  │  ├─ 汇总为统一主观评分结果（含 reason/confidence/status）
│  │  ├─ 失败/超时时按维度降级（不拖垮整批）
│  │  └─ 返回可直接写入 score 的结构
│  │
│  ├─ ds_config.py                                [新增，建议]
│  │  ├─ 读取 DS_API_KEY / BASE_URL / MODEL / TIMEOUT
│  │  ├─ 并发数、重试次数、是否启用的配置
│  │  └─ 配置校验（缺 key 时给出清晰错误）
│  │
│  ├─ requirements.txt                            [修改（按实际依赖）]
│  │  ├─ 若无 httpx：新增 httpx
│  │  └─ （可选）python-dotenv
│  │
│  ├─ .env                                        [新增，本地使用，不提交]
│  │  └─ DS_API_KEY / DS_BASE_URL / DS_MODEL 等
│  │
│  └─ .env.example                                [新增，建议提交]
│     └─ 给协作者的配置模板
│
├─ frontend/
│  ├─ matching.html                               [修改]
│  │  ├─ 新增“启用 DS 主观评分”开关
│  │  ├─ 新增 DS 调用状态提示（启用/禁用）
│  │  ├─ 候选对/匹配结果表增加分维度主观分列
│  │  ├─ 增加 llm_reason（折叠展示即可）
│  │  └─ summary 显示 llm_stats（调用/失败/超时）
│  │
│  └─ （可选）ds_debug.html                       [新增，可选]
│     └─ 单独测试单个师生 pair 的 DS 输出（后期调 prompt 很有用）
│
├─ 开发记录.md                                    [修改，强烈建议]
│  ├─ 明确三个主观维度的职责边界（防止重复计分）
│  ├─ 写清每个 prompt 的评分范围与扣分范围
│  ├─ 写清 DS 失败/超时的降级策略
│  └─ 写清最终总分公式（便于你后续答辩/文档）
│
├─ .gitignore                                     [修改，强烈建议]
│  ├─ 加入 .env
│  ├─ 加入日志/缓存文件（如有）
│  └─ 继续忽略 __pycache__ / *.pyc / *.zip / ~$*.docx 等
│
└─ tests/                                         [可选但推荐]
   ├─ test_ds_scoring_service.py                  [新增]
   │  ├─ 测试 JSON 解析成功
   │  ├─ 测试字段缺失时降级
   │  └─ 测试超范围分数被裁剪
   └─ test_stage2_pipeline.py                     [新增]
      └─ 测试 llm_enabled 开关对结果结构的影响





      匹配与调度（目标版本）
├─ backend
│  ├─ .env.example                         [改] DS配置项补充说明（缓存/重试相关可选）
│  ├─ .gitignore                           [保留]
│  ├─ app.db                               [运行生成]
│  ├─ database.py                          [保留]
│  ├─ models.py                            [改]
│  │                                        # 学生/老师表 + 合法pair评分明细表 + 最终匹配结果表
│  │                                        # 未匹配学生表 + 未使用老师表 + DS缓存表
│  │                                        # （旧MatchJob/MatchResult可保留但本期停用）
│  ├─ schemas.py                           [改]
│  │                                        # 导入preview/commit响应模型、学生/老师列表输出模型
│  ├─ validators.py                        [改]
│  │                                        # 无表头固定列校验（学生16列、老师18列）
│  │                                        # 行解析/类型转换/重复行识别（文件内）
│  ├─ import_service.py                    [改]
│  │                                        # xlsx无表头解析、preview、commit数据准备、错误保留
│  ├─ ds_config.py                         [保留/小改]
│  ├─ ds_client.py                         [保留/小改]
│  ├─ ds_prompt_templates.py               [改]
│  │                                        # 2套prompt：主观匹配(40) / 特殊需求惩罚(20)
│  ├─ ds_scoring_service.py                [改]
│  │                                        # 两类DS评分、结构化解析、重试2次、失败回退
│  ├─ ds_cache_service.py                  [新]
│  │                                        # DB缓存读写（pair + score_type + input_hash）
│  ├─ matching_rules.py                    [改]
│  │                                        # 硬约束过滤（性别+线上线下）
│  │                                        # grade/subject客观分计算、总分公式
│  ├─ matching_schemas.py                  [改/重写]
│  │                                        # 完整匹配运行结果摘要、合法pair明细、最终结果、
│  │                                        # 未匹配学生/未使用老师、前端展示模型
│  ├─ matching_service.py                  [改/重写]
│  │                                        # 生成合法pair、客观打分、DS评分、分数合成、排序
│  │                                        # 阶段内两轮匹配（优先学生→普通学生）
│  ├─ matching_orchestrator.py             [改/重写]
│  │                                        # 自动完整流程：clear旧结果 -> direct阶段 -> pre阶段 -> 落库
│  ├─ matching_store.py                    [改/重写]
│  │                                        # 当前结果查询：最终匹配/合法pair明细/未匹配学生/未使用老师
│  ├─ export_service.py                    [新]
│  │                                        # 导出xlsx（4个sheet）
│  ├─ main.py                              [改/重构收口]
│  │                                        # 路由收口：导入、列表、完整匹配运行、结果查询、导出
│  │                                        # 清理重复路由/旧run-full jobs接口冲突
│  ├─ run.py                               [保留]
│  ├─ requirements.txt                     [改]
│  │                                        # 确认openpyxl等依赖
│  ├─ start_backend.bat                    [保留]
│  │
│  ├─ matching_main.py                     [停用/待清理]
│  │                                        # 本期不作为入口（避免双入口混乱）
│  ├─ matching_store_legacy.py             [无]（不新增，仅说明不走历史任务方向）
│  ├─ matching_rules_legacy.py             [无]
│  └─ validators_legacy.py                 [无]
│
├─ frontend
│  ├─ students.html                        [改]
│  │                                        # 学生无表头导入预览+commit；列表字段对齐新Excel结构
│  ├─ volunteers.html                      [改]
│  │                                        # 老师无表头导入预览+commit；列表字段对齐新Excel结构
│  ├─ matching.html                        [大改]
│  │                                        # 一页完成：运行完整匹配、展示4类结果、导出按钮
│  │                                        # （方案X：不新增结果页面）
│  ├─ common.js                            [新，建议]
│  │                                        # fetch封装、日志、表格渲染辅助、下载导出
│  └─ 1.png                                [保留]
│
├─ 开发记录.md                              [改，建议]
│                                           # 补一版“系统定稿规格 + 接口约定 + 开发阶段记录”
└─ .gitignore                               [保留]


批次 1：后端基线重写（可启动 + 可导入）

目标：先把后端跑起来，导入链路稳定。

文件（完整重写/大改）

backend/models.py

backend/schemas.py

backend/validators.py

backend/import_service.py

backend/main.py（先接导入+列表基础接口）

（小改）backend/requirements.txt

验收

后端可启动

学生/老师导入 preview + commit 正常

列表查询正常

无重复路由/导入冲突

批次 2：匹配核心 + DS缓存 + 结果落库

目标：完整匹配逻辑跑通（不含前端展示细化）。

文件

backend/matching_rules.py

backend/ds_prompt_templates.py

backend/ds_scoring_service.py

backend/ds_cache_service.py（新）

backend/matching_schemas.py

backend/matching_service.py

backend/matching_orchestrator.py

backend/matching_store.py

backend/main.py（补匹配运行与结果查询接口）

验收

点击接口可完成 direct→pre 全流程

结果落库（合法pair明细/最终结果/未匹配学生/未使用老师）

DS失败回退不阻塞任务

特殊需求为“无”时不调第二次DS

批次 3：导出与前端页面重写（方案X）

目标：前端一页完成运行、查看、导出。

文件

backend/export_service.py（新）

backend/main.py（补导出接口）

frontend/students.html

frontend/volunteers.html

frontend/matching.html（大改）

frontend/common.js（新，建议）

验收

前端可运行完整匹配

可展示4类结果数据

可导出4-sheet xlsx

批次 4：收口与清理（可选但建议）

目标：把旧代码影响降到最低，项目进入稳定状态。

文件

backend/ds_config.py（小改，补注释/默认项）

backend/ds_client.py（小改，统一返回结构）

开发记录.md（更新定稿规格与接口）

清理/停用 matching_main.py 等旧入口说明

API key : 

python -m pip install --upgrade pip
python -m pip install "httpx<0.28"
http的级别降低一点

python -m pip install --upgrade pip
python -m pip install "httpx<0.28"

$env:DEEPSEEK_API_KEY = ""
echo $env:DEEPSEEK_API_KEY

python -m uvicorn main:app --host 127.0.0.1 --port 8000

cd "D:\college\research_archive\数星阁-卓越杯\匹配与调度（0117-0125）\frontend"
python -m http.server 5500

后端
http://127.0.0.1:8000/docs


前端
http://127.0.0.1:5500/students.html

http://127.0.0.1:5500/volunteers.html

http://127.0.0.1:5500/matching.html