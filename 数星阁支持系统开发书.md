# 师生匹配系统开发描述细则

## 0. 文档用途

本文档用于指导“师生匹配系统”的完整开发与协作。目标是让任何新加入的开发者（或新的 AI 合作者）在不额外口头沟通的情况下，准确理解系统功能、数据结构、匹配规则、打分逻辑、DS 调用方式、前端展示与导出要求，并据此进行实现。

---

## 1. 项目目标与范围

## 1.1 项目目标

开发一个**管理员使用的内部工具型师生匹配系统**，支持：

1. 导入学生 Excel / 老师（volunteer）Excel
2. 数据预览与导入落库
3. 生成合法师生匹配对
4. 对合法匹配对进行打分（含 DeepSeek 主观评分）
5. 自动执行完整匹配（`direct` 阶段 → `pre` 阶段）
6. 结果落库（含匹配结果、未匹配学生、未使用老师、合法对打分明细）
7. 前端页面展示结果
8. 导出 Excel（多个 sheet）

---

## 1.2 本期系统角色与权限范围

* **唯一角色：管理员**
* **不做登录鉴权**
* 管理员负责导入、运行匹配、查看结果、导出结果

---

## 1.3 本期明确不做的功能（非目标）

以下功能本期不开发：

* 登录鉴权（管理员/多角色）
* 手动新增/编辑学生与老师信息
* 人工调剂匹配结果
* 审核/发布状态流转
* 操作日志/审计日志（系统级）
* 历史任务列表/历史任务详情页（本期只保留“当前结果”）
* 导出 DS 原文（数据库保存原文，但导出仅摘要）

---

# 2. 整体业务流程（系统自动流程）

## 2.1 主流程（管理员视角）

1. 导入学生 Excel（预览 → 提交落库）
2. 导入老师 Excel（预览 → 提交落库）
3. 点击“运行完整匹配”
4. 系统自动执行：

   * `direct` 老师池 + 全体学生：打分 + 两轮匹配（优先学生→普通学生）
   * 若还有剩余学生：`pre` 老师池 + 剩余学生：打分 + 两轮匹配（优先学生→普通学生）
5. 落库：

   * 合法 pair 打分细则（含 DS 分项、理由原文）
   * 最终匹配结果
   * 未匹配学生
   * 未使用老师
6. 前端展示上述结果
7. 导出 Excel（4个 sheet）

---

## 2.2 运行策略（重跑行为）

* **每次重新运行匹配前，清空旧结果（方案1）**
* 清空的内容包括（至少）：

  * 合法 pair 打分细则表
  * 最终匹配结果表
  * 未匹配学生表
  * 未使用老师表
* 学生表 / 老师表不清空（除非管理员主动重新导入覆盖逻辑，本期导入是“重复序号跳过”）

---

# 3. Excel 输入规范（严格按列顺序，无表头）

> **非常重要：Excel 无表头，系统按固定列序解释。**
> 输入方保证编码正确；系统不做复杂语义纠错，但会做基础格式/列数检查。

---

## 3.1 学生 Excel 规范（固定16列）

### 文件要求

* 文件类型：`.xlsx`
* 单文件单 sheet
* 无表头
* 不支持备注行/空行/合并单元格（输入需规范）

### 列顺序与字段定义（学生）

1. **序号**
2. **名字**
3. **性别**（`M` / `F`）
4. **学段**（编码：`1`=小学，`2`=初中，`3`=高中）
5. **线上/线下**（`offline` / `online` / `both`）
6. **偏好科目1**（编码 `0~9`，`0` 表示无）
7. **偏好科目2**
8. **偏好科目3**
9. **学生情况描述**
10. **学习风格描述**
11. **学生兴趣爱好**
12. **学生性格**
13. **社工姓名**
14. **社工电话**
15. **是否有优先**（`0/1`，`1`表示优先）
16. **特殊需求**

### 学生端补充约定

* 若偏好科目不足3个，用 `0` 补位
* 文本类字段无内容时填 `"无"`
* 学生对老师**没有性别要求**
* 学生**没有匹配模式字段**

---

## 3.2 老师（volunteer）Excel 规范（固定18列）

### 文件要求

* 文件类型：`.xlsx`
* 单文件单 sheet
* 无表头
* 不支持备注行/空行/合并单元格

### 列顺序与字段定义（老师）

1. **序号**
2. **姓名**
3. **性别**（`M` / `F`）
4. **学号**
5. **学邮**
6. **院系**
7. **辅导方式**（`offline` / `online` / `both`）
8. **匹配模式**（`direct` / `pre`，全小写）
9. **是否参加过过去的活动**（`0/1`）
10. **个人性格**
11. **对学生的性别要求**（`F` / `M` / `N`；`N`表示无要求）
12. **偏好科目1**（编码 `0~9`）
13. **偏好科目2**
14. **偏好科目3**
15. **偏好学段1**（编码 `1/2/3`）
16. **偏好学段2**
17. **偏好学段3**
18. **可带学生数**（本期固定按 `1` 使用）

### 老师端补充约定

* 科目偏好不足3个，用 `0` 补位
* 偏好学段输入保证不为空、不重复
* 老师容量字段虽存在，但本期算法按 **1对1** 实现，容量统一视为1

---

## 3.3 科目编码约定（系统不做中文映射，仅按编码使用）

科目编码固定如下（由输入 Excel 保证正确）：

* `1` 语文
* `2` 数学
* `3` 英语
* `4` 物理
* `5` 化学
* `6` 生物
* `7` 政治
* `8` 历史
* `9` 地理
* `0` 无偏好（占位）

> 系统计算只使用编码，不做中文名称映射逻辑（展示层可选做映射）。

---

# 4. 导入与校验规则

## 4.1 导入模式

* 支持“预览（preview）+ 提交（commit）”两步
* **不需要严格/非严格导入模式**（本期不做 strict/non-strict）

---

## 4.2 基础校验（导入阶段）

虽然输入方保证编码正确，但系统仍需做基础校验以防列错位：

### 学生表基础校验

* 每行列数必须为 **16**
* 行可解析为预期类型（序号、编码、布尔等）
* 否则该行标记为导入错误（预览可见）

### 老师表基础校验

* 每行列数必须为 **18**
* 行可解析为预期类型
* 否则该行标记为导入错误

---

## 4.3 重复序号处理（定稿）

### 情况1：Excel 文件内部重复序号

* **保留第一条**
* 后续重复序号行跳过

### 情况2：数据库中已存在该序号

* **跳过**
* 不覆盖更新

---

# 5. 合法匹配对生成规则（硬约束过滤）

## 5.1 硬约束只有两项

1. **性别要求**
2. **线上/线下匹配**

除此之外（学段、科目、专业、特殊需求等）均不做硬过滤，进入打分环节处理。

---

## 5.2 线上/线下匹配合法性（学生端与老师端都有 3 值）

学生与老师辅导方式字段值域均为：

* `offline`
* `online`
* `both`

### 不合法情况（仅两种）

* 学生 = `offline` 且 老师 = `online`
* 学生 = `online` 且 老师 = `offline`

### 其他情况全部合法

包括任一方为 `both` 的情况。

---

## 5.3 性别要求合法性

* 学生端对老师**无性别要求**
* 老师端字段“对学生的性别要求”为 `F/M/N`

  * `N`：无要求（所有学生性别均合法）
  * `F`：仅学生性别为 `F` 合法
  * `M`：仅学生性别为 `M` 合法

---

# 6. 打分体系（客观分 + DS 主观分 + 特殊需求惩罚）

## 6.1 每个合法 pair 的最终总分公式（定稿）

```text
total_score = grade_score + subject_score + ds_subjective_score - ds_special_penalty
```

### 分值范围

* `grade_score ∈ [0, 20]`
* `subject_score ∈ [0, 40]`
* `ds_subjective_score ∈ [0, 40]`
* `ds_special_penalty ∈ [0, 20]`

### 说明

* **总分允许为负数**
* **不做下限截断**
* 所有分数保留 **2位小数**

---

## 6.2 客观分一：学段匹配分（grade_score，20分）

### 规则

学生的学段编码（1/2/3）与老师偏好学段1/2/3进行匹配：

* 命中老师偏好学段1 → `20.00`
* 命中老师偏好学段2 → `13.33`（20 × 2/3）
* 命中老师偏好学段3 → `6.67`（20 × 1/3）
* 未命中 → `0.00`

### 输入保证

* 学生学段已是编码 `1/2/3`
* 老师偏好学段不为空且不重复（由输入保证）

---

## 6.3 客观分二：科目匹配分（subject_score，40分）

### 偏好赋分（学生与老师相同）

* 偏好1 → `3`
* 偏好2 → `2`
* 偏好3 → `1`
* `0` 表示无偏好，不参与有效匹配

### 计算方法（定稿）

对于一个合法师生对：

1. 学生3个偏好科目 × 老师3个偏好科目，共形成 **9个组合**
2. 对每个组合：

   * 如果科目编码相同且非0，则该组合原始分 = `学生优先级分 × 老师优先级分`
   * 否则该组合分 = `0`
3. 取 9 个组合中的最大值：

   * `best_subject_raw ∈ [0,9]`
4. 映射到40分：

   * `subject_score = round(40 * best_subject_raw / 9, 2)`

### 说明

* 使用“**取最高科目命中**”方案，不做多科累加
* 输入保证科目编码正确
* `0` 仅作占位，不应参与有效科目匹配

---

## 6.4 DS 主观评分（第一类 DS 调用，40分）

### 目标

评估师生主观匹配度，综合考虑：

* 学习风格
* 性格
* 教学风格适配（通过老师主观字段推断）
* 兴趣爱好相关性
* 老师专业背景与学生情况的匹配性（并入该40分总项）

### 分值

* `ds_subjective_score ∈ [0, 40]`

### DS 输入字段（固定）

#### 学生侧（仅使用第9~12列）

* 学生情况描述（第9列）
* 学习风格描述（第10列）
* 学生兴趣爱好（第11列）
* 学生性格（第12列）

#### 老师侧（仅使用以下3项）

* 院系（专业）
* 是否参加过过去的活动
* 个人性格

### DS 输出要求（建议结构化）

至少返回：

* `score`（0~40）
* `reason_summary`（简短摘要，用于前端展示/导出）
* `reason_raw`（原始解释文本，用于落库审计/追踪）
* `status`（success/fallback/error）
* `error_message`（若失败）

> 注：具体 prompt 可由实现者设计，但必须稳定输出结构化结果（建议 JSON）。

---

## 6.5 DS 特殊需求惩罚（第二类 DS 调用，20分）

### 目标

评估“老师与学生特殊需求的不匹配程度”，作为**惩罚分**

* 越不匹配，惩罚越高
* 匹配良好则惩罚低

### 分值

* `ds_special_penalty ∈ [0, 20]`

### DS 输入字段（固定）

#### 学生侧

* 特殊需求（第16列）

#### 老师侧

* **老师全部字段**（完整信息）

### 特殊需求为空时（重要）

* 若学生“特殊需求”为空/无有效内容（例如 `"无"`），则：

  * **不调用 DS**
  * `ds_special_penalty = 0`
  * 理由可记录为：`无特殊需求，惩罚分置0`

### DS 输出要求（建议结构化）

至少返回：

* `penalty_score`（0~20）
* `reason_summary`
* `reason_raw`
* `status`
* `error_message`

---

# 7. DeepSeek 调用策略（缓存、失败回退、批处理）

## 7.1 调用次数

* 每个合法 pair 最多调用 DS **2次**

  1. 主观匹配度（40分）
  2. 特殊需求惩罚（20分；若学生无特殊需求则跳过）

---

## 7.2 调用方式要求

* 采用“**批处理执行策略**”（系统层分批调度）
* 接受运行时间较长（规模小于50×50）
* 必须缓存（见下）

> 说明：如果 DS API 不支持原生批量请求，则在应用层按 pair 分批调度调用（可串行或受控并发），本质上仍属于“批处理执行”。

---

## 7.3 缓存要求（必须，数据库缓存）

### 缓存目标

避免同一师生 pair 在输入未变化时重复调用 DS，提高稳定性与效率。

### 缓存介质

* **数据库缓存（必须）**

### 缓存命中规则（定稿）

缓存键建议至少包含：

* `student_seq`
* `teacher_seq`
* `score_type`（`subjective` / `special`）
* **输入文本哈希**（hash）

### 输入文本哈希设计原则

* 若用于 DS 的输入字段文本未变化，则命中缓存
* 若师生信息变化导致输入文本变化，则哈希变化，自动重新调用 DS

---

## 7.4 DS 失败处理策略（定稿）

* 每次 DS 调用失败后进行重试
* **重试次数 N = 2**
* 超过重试次数仍失败时：

  * 保留错误信息
  * 任务继续（不终止整次匹配）

### 回退分数策略

* 主观匹配度调用失败 → `ds_subjective_score = 0`
* 特殊需求惩罚调用失败 → `ds_special_penalty = 0`

---

# 8. 匹配算法（1对1，自动两阶段 + 每阶段两轮）

## 8.1 匹配单位

* **固定 1 对 1**
* 一个学生最多匹配一个老师
* 一个老师最多匹配一个学生（本期容量统一视为1）

---

## 8.2 总体匹配阶段（系统自动完成）

系统无需管理员手动分阶段操作，自动执行：

### 阶段1：`direct`

* 使用所有学生
* 使用所有 `匹配模式 = direct` 的老师
* 生成合法 pair → 打分 → 排序 → 两轮匹配

### 阶段2：`pre`

* 若阶段1后仍有剩余学生
* 使用剩余学生
* 使用所有 `匹配模式 = pre` 的老师
* 生成合法 pair → 打分 → 排序 → 两轮匹配

---

## 8.3 每个阶段内部的两轮匹配（优先学生优先）

假设某阶段已得到按规则排序的合法 pair 列表：

### 第一轮（优先学生轮）

遍历排序后的 pair 列表：

* 若当前 pair 的学生 `是否有优先 = 1`，且该学生和该老师均尚未匹配：

  * 选择该 pair
  * 将该学生、该老师标记为已匹配
* 若当前 pair 学生不是优先学生：

  * **跳过，保留到第二轮**
* 若当前 pair 任一方已匹配：

  * 跳过（不选）

### 第二轮（普通学生轮）

再次遍历排序后的 pair 列表：

* 对尚未匹配双方的 pair，按排序顺序选择
* 直到列表遍历完毕

---

## 8.4 Pair 排序与同分规则（定稿）

### 主要排序键

1. `total_score` 降序（高分优先）

### 同分时（用户定稿）

2. `teacher_seq` 升序（老师序号小优先）

### 为保证完全可复现，建议补充稳定排序键（实现要求）

3. `student_seq` 升序（当同分且 teacher_seq 相同时使用）

> 用户明确规则是“同分时老师序号小优先”；为避免完全相同 teacher_seq 的 pair 顺序不稳定，工程实现中应增加 `student_seq` 作为次级稳定键。

---

# 9. 落库范围与数据存储要求（本期必须）

本期需要落库并在前端展示（共6类）：

1. 学生表
2. 老师表
3. 所有合法匹配对的打分细则（含 DS 分项与理由原文）
4. 最终匹配结果
5. 未匹配学生
6. 未使用老师

---

## 9.1 合法 pair 打分细则表（核心）

### 存储范围（按阶段参与计算）

采用“按实际参与计算落库”的方案，即：

* `direct` 阶段：`全部学生 × direct老师` 的合法 pair（含打分结果）
* `pre` 阶段：`剩余学生 × pre老师` 的合法 pair（含打分结果）

并为每条明细记录标记：

* `phase = direct / pre`

> 不要求一次性落库“全体学生×全体老师”的合法 pair。

---

## 9.2 合法 pair 打分细则表建议字段（必须覆盖的信息）

建议至少包含以下字段（名称可调整）：

### 基本标识

* `id`
* `phase`（direct/pre）
* `student_seq`
* `student_name`
* `teacher_seq`
* `teacher_name`

### 匹配基础信息（便于前端展示/调试）

* 学生辅导方式（online/offline/both）
* 老师辅导方式（online/offline/both）
* 学生是否优先（0/1）
* 老师匹配模式（direct/pre）

### 客观分明细

* `grade_score`
* `subject_score`
* `best_subject_raw`（0~9，可选但推荐）
* `objective_score_subtotal`（可选）

### DS 主观匹配（40分）

* `ds_subjective_score`
* `ds_subjective_reason_summary`（前端/导出使用）
* `ds_subjective_reason_raw`（数据库保存原文）
* `ds_subjective_status`
* `ds_subjective_error_message`（如有）
* `ds_subjective_cache_hit`（0/1，推荐）
* `ds_subjective_input_hash`（推荐）

### DS 特殊需求惩罚（20分）

* `ds_special_penalty`
* `ds_special_reason_summary`
* `ds_special_reason_raw`
* `ds_special_status`
* `ds_special_error_message`
* `ds_special_cache_hit`（推荐）
* `ds_special_input_hash`（推荐）

### 总分与结果辅助

* `total_score`
* `selected_in_final`（0/1，可选，便于前端联动）
* `not_selected_reason`（可选，例如“老师已被更高分pair占用”）

---

## 9.3 最终匹配结果表

每条记录表示一条最终选中的师生匹配。

建议至少包含：

* `student_seq`, `student_name`
* `teacher_seq`, `teacher_name`
* `phase`（direct/pre）
* `student_mode`（学生无匹配模式，可不存）
* `teacher_mode`（direct/pre，可选，通常等于phase）
* `tutoring_mode`（最终匹配的辅导方式相关信息，可展示老师方式）
* 分项分：

  * `grade_score`
  * `subject_score`
  * `ds_subjective_score`
  * `ds_special_penalty`
* `total_score`
* `ds_subjective_reason_summary`
* `ds_special_reason_summary`

> 前端“匹配结果页”显示信息以此表为主。

---

## 9.4 未匹配学生表

存储本次运行后未成功匹配的学生（当前结果）。

建议至少包含：

* `student_seq`
* `student_name`
* `gender`
* `stage_code`
* `tutoring_mode`
* `is_priority`
* `reason_summary`（例如：无合法老师 / direct和pre均未匹配成功）

---

## 9.5 未使用老师表

存储本次运行后未被使用的老师（当前结果）。

建议至少包含：

* `teacher_seq`
* `teacher_name`
* `match_mode`（direct/pre）
* `tutoring_mode`
* `reason_summary`（例如：无可匹配学生 / 在竞争中未被选中）

---

## 9.6 DS 缓存表（数据库缓存，必须）

用于缓存 DS 评分结果，避免重复调用。

建议字段：

* `id`
* `student_seq`
* `teacher_seq`
* `score_type`（subjective/special）
* `input_hash`
* `score_value`
* `reason_summary`
* `reason_raw`
* `status`
* `error_message`
* `created_at`
* `updated_at`

唯一索引建议：

* `(student_seq, teacher_seq, score_type, input_hash)`

---

# 10. 前端页面与展示要求（本期）

## 10.1 页面结构（方案X，定稿）

保留并扩展现有三页：

1. **学生管理页**

   * 导入预览 + commit
   * 学生列表展示（当前库中学生）

2. **老师管理页**

   * 导入预览 + commit
   * 老师列表展示（当前库中老师）

3. **匹配页面（核心）**

   * 运行完整匹配按钮（系统自动 direct→pre）
   * 展示与导出结果（多个区块/标签）

---

## 10.2 匹配页面需展示的内容（六类数据中的结果相关）

### A. 最终匹配结果（重点展示）

显示字段（用户已明确）：

* 学生序号
* 学生姓名
* 老师序号
* 老师姓名
* 辅导方式
* 总分
* 分项分

  * grade
  * subject
  * ds主观匹配（40）
  * ds特殊需求惩罚（20）
* DS 打分解释说明（摘要）
* 阶段标记（direct/pre，建议展示）

---

### B. 合法 pair 打分明细（调试/分析）

展示至少：

* 学生/老师序号与姓名
* phase
* 分项分与总分
* DS摘要
* 是否缓存命中（可选）
* DS状态（可选）

---

### C. 未匹配学生列表

展示学生基本信息 + 原因摘要

---

### D. 未使用老师列表

展示老师基本信息 + 原因摘要

---

# 11. Excel 导出要求（本期必须）

导出一个 `.xlsx` 文件，包含 4 个 sheet：

1. **最终匹配结果表**
2. **未匹配学生表**
3. **未使用老师表**
4. **全部合法 pair 打分明细**

---

## 11.1 导出内容与 DS 文本要求

* 导出中包含：

  * 分数
  * **DS 理由摘要**
* 导出中**不包含 DS 原文**

  * 原文只保存在数据库（合法 pair 打分明细 / 缓存记录中）

---

# 12. API 与后端行为要求（实现层约束）

> 以下为行为要求，不强制具体框架实现方式（当前项目为 FastAPI 风格可沿用）。

## 12.1 导入相关

* 学生导入预览接口（xlsx上传）
* 学生导入提交接口（commit）
* 老师导入预览接口
* 老师导入提交接口

### 导入预览输出需包含

* 解析成功行
* 错误行（如列数错误/类型解析失败）
* 重复序号行（文件内重复）
* 可提交数量统计

---

## 12.2 匹配运行接口（完整匹配）

提供“完整匹配”接口，点击后系统自动执行：

* 清空旧结果
* `direct`阶段完整流程
* `pre`阶段完整流程（仅剩余学生）
* 落库四类结果
* 返回运行结果摘要（如匹配数、未匹配数、DS调用统计等）

---

## 12.3 结果查询接口（当前结果）

提供接口供前端获取：

* 最终匹配结果
* 合法 pair 打分细则
* 未匹配学生
* 未使用老师

> 不需要历史任务接口。

---

## 12.4 导出接口

提供导出接口，返回 `.xlsx` 文件下载（四个sheet）。

---

# 13. 实现细节建议（工程层，非业务规则）

## 13.1 DS Prompt 设计要求（关键）

应拆分为两套 prompt（与两次调用对应）：

1. **主观匹配度评分 Prompt**

   * 输入学生主观字段（9~12列）+ 老师的院系/是否参加过活动/个人性格
   * 输出 0~40 分 + 理由（摘要 + 原文）

2. **特殊需求惩罚评分 Prompt**

   * 输入学生特殊需求 + 老师全部字段
   * 输出 0~20 惩罚分 + 理由（摘要 + 原文）
   * 若学生特殊需求为“无”，系统层直接跳过，不调用 DS

### Prompt 输出格式

建议强制 JSON 输出，便于稳定解析。

---

## 13.2 分数与排序精度

* 所有分数保留 2 位小数
* 排序使用真实数值（非字符串）
* 排序键建议明确为：

  * `(-total_score, teacher_seq, student_seq)`

> 其中 `teacher_seq` 与 `student_seq` 若为字符串编号，需统一比较策略（推荐标准化为可比较形式）。

---

## 13.3 当前结果模式（无历史任务）

由于本期无历史任务页面，建议后端设计为“当前结果快照模式”：

* 每次运行覆盖当前结果表
* 导出与前端展示均读取当前结果表

---

## 13.4 规模与性能预期

* 学生数 ≤ 50
* 老师数 ≤ 50
* 接受运行时间较长（无严格上限）
* 重点保证：

  * 正确性
  * 可解释性
  * DS调用可复用（缓存）
  * 失败可继续（不中断整批）

---

# 14. 验收标准（建议作为开发完成判定）

## 14.1 导入验收

* 能成功导入符合格式的学生/老师 `.xlsx`
* 重复序号按规则跳过（保留第一条）
* 列数错误/类型错误行可在预览中识别

## 14.2 合法对与打分验收

* 合法 pair 仅按“性别要求 + 线上线下”过滤
* grade/subject 计算与规则一致
* DS主观分/特殊惩罚分正确接入
* 特殊需求为“无”时不调用第二次DS
* DS失败可回退并继续任务

## 14.3 匹配验收

* 自动完成 `direct -> pre` 两阶段
* 每阶段内部“优先学生轮 → 普通学生轮”生效
* 同分时老师序号小优先（并保持结果可复现）

## 14.4 结果落库与展示验收

* 前端可查看：

  * 最终匹配结果
  * 合法pair打分明细
  * 未匹配学生
  * 未使用老师
* 数据与实际运行结果一致

## 14.5 导出验收

* 成功导出包含4个sheet的 `.xlsx`
* 导出包含分数与DS摘要，不包含DS原文

---

# 15. 术语与关键约定（避免误解）

* **老师“优先级”**：本项目中不使用该术语
  老师只有：

  * `匹配模式`：`direct` / `pre`
* **学生“优先”**：指字段 `是否有优先`（`0/1`）
* **特殊需求分**：是**惩罚分**（分数越高表示越不匹配）
* **合法 pair 打分细则**：包含分项分、总分、DS摘要与DS原文（数据库保存）

---
